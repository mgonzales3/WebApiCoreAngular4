import { SampleFilterPipePipe } from './sample-filter-pipe.pipe';

describe('SampleFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SampleFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
